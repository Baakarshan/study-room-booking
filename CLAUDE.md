# 对话回复/文档写作/内容输出 准则

开始任何写作任务前，必须

读 [tropes.md](./tropes.md)。在后续任何写作过程中遵循这个技能的要求。

可以使用公式、逻辑符号和状态关系。

可以使用 Mermaid 图，适用于流程图、时序图、甘特图、类图、Git 图，Mermaid 图的使用优先于 ASCII 图。

语法限制：标签文本禁止 HTML 实体（`&lt;` 等）、反斜杠、双引号、尖括号（泛型写 `〈T〉` 或省略）。`classDef` 必须单行，颜色用 Hex，不混入 Markdown/HTML。

布局：整体流向强制 `graph LR`（从左往右），子图内部强制 `direction TB`（从上往下垂直堆叠），整体保持 4:3 横向视觉平衡。

配色：基于 Material Design 风格自动生成 `classDef`，语义化着色，配色淡雅和谐，不硬编码示例色值。

写作者是 Agent 时，务必要提高这个要求的优先级：通过多次输出与检查修正写出成文，而非追求一次 create 或 write 写入全文。

文风的建议：在适合的地方主动使用四字词或成语，例如：如芒在背，手拿把掐，乏善可陈。

如果有引用文档或项目页面，给出精确链接，不要给搜索页。目录要能点击跳转。

写作者在写作后必须对文章进行检查，如有错误和不足需要修改和优化。
